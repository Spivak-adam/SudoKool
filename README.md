# SudoKool
A web-based Sudoku in with a Angular front end, ASP .NET C# backend, and a SQL server DB

Open one local terminal and run:
cd 'angular/sodukool'

then run
'ng serve'

Open a second terminal and run:
cd '.\C#\SudoKool Server\'

then run
dotnet run